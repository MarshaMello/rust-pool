// match expression
// using math expression on adding logic on app

fn main () {
    let my_bool = true;
    match my_bool {
        true => println!("this is true"),
        false => println!("this is false"),
    }
}